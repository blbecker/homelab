apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openclaw-operator
  namespace: openclaw-operator
spec:
  chartRef:
    kind: OCIRepository
    name: openclaw-operator
    namespace: flux-system
  interval: 5m
  timeout: 5m
  releaseName: openclaw-operator
  values: # https://artifacthub.io/packages/helm/openclaw-operator/openclaw-operator?modal=values
