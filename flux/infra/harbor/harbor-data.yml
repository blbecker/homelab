---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: harbor-qnap
  namespace: harbor
  annotations:
    nfs.io/storage-path: "harbor-data"
spec:
  storageClassName: qnap-fixed-name-nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
