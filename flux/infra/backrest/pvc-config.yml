apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backrest-config
  namespace: backrest
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
