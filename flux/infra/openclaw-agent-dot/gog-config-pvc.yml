apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gog-config
  namespace: openclaw-operator
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi
