apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: main-ro
  namespace: cloudnative-pg
spec:
  cluster:
    name: main

  instances: 1
  type: ro
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "1000"
      default_pool_size: "10"
