apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-ddns
  namespace: cloudflare-ddns
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflare-ddns
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloudflare-ddns
    spec:
      securityContext:
        fsGroup: 2000
        fsGroupChangePolicy: "Always"
        readOnlyRootFilesystem: true
        runAsUser: 2000
        runAsGroup: 2000
      dnsConfig:
        options:
          - name: ndots
            value: "1"
      containers:
        - name: cloudflare-ddns
          image: docker.io/favonia/cloudflare-ddns:1.16.2
          securityContext:
            capabilities:
              drop:
                - ALL
                - NET_RAW
              add:
                - SETUID
                - SETGID
          env:
            - name: TZ
              value: "America/Puerto_Rico"
            - name: PUID
              value: "3000"
            - name: PGID
              value: "3000"
            - name: CF_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflare-api-token
                  key: api-token
            - name: DOMAINS
              value: home.tartarus.us
            - name: PROXIED
              value: "false"
            - name: UPDATE_CRON
              value: "@every 5m"
            - name: IP6_PROVIDER
              value: none
          podSecurityContext:
            fsGroup: 3000
            fsGroupChangePolicy: "Always"
          resources:
            requests:
              memory: "50M"
              cpu: ".1"
            limits:
              memory: "1G"
              cpu: "2"
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: cloudflare-ddns
          #   initialDelaySeconds: 10
          #   timeoutSeconds: 10
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: cloudflare-ddns
          #   initialDelaySeconds: 10
          #   timeoutSeconds: 5
