apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: knot-data
  namespace: knot
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
