apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grampsweb-index
  namespace: grampsweb
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
