apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grampsweb-secret
  namespace: grampsweb
  annotations:
    nfs.io/storage-path: "gramps-secret"
spec:
  storageClassName: qnap-fixed-name-nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
