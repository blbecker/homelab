apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grampsweb-tmp
  namespace: grampsweb
  annotations:
    nfs.io/storage-path: "gramps-tmp"
spec:
  storageClassName: qnap-fixed-name-nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
