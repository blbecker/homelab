apiVersion: v1
kind: Service
metadata:
  name: grampsweb
  namespace: grampsweb
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: grampsweb
  ports:
    - name: grampsweb
      port: 5000
      targetPort: grampsweb
      protocol: TCP
