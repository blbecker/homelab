apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grampsweb-users
  namespace: grampsweb
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Mi
