apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grampsweb-config
  namespace: grampsweb
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Mi
