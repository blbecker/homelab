apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: calibre-web
  namespace: calibre
  annotations:
    switchboard.borchero.com/ignore: "all"
    external-dns.alpha.kubernetes.io/target: traefik.tartarus.us
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`calibre-web.tartarus.us`)
      kind: Rule
      services:
        - name: calibre-web
          kind: Service
          port: calibre-web
      middlewares:
        - name: https-redirect-middleware
          namespace: calibre

  tls:
    secretName: letsencrypt-wildcard-cert-tartarus.us-prod
