apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-config
  namespace: actual
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
