apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-media-qnap
  namespace: jellyfin
  annotations:
    nfs.io/storage-path: "media"
spec:
  storageClassName: qnap-fixed-name-nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
