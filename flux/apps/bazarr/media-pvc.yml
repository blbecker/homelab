apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-media-qnap
  namespace: bazarr
  annotations:
    nfs.io/storage-path: "media"
spec:
  storageClassName: qnap-fixed-name-nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
