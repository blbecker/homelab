apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-data
  annotations:
    nfs.io/storage-path: "authentik/data"
spec:
  storageClassName: qnap-fixed-name-nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
