apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apprise-attachments
  namespace: apprise
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
